import { EducationType, ThemePropType } from "@/types";

export const EducationCard = ({
  education,
  theme,
}: ThemePropType & { education: EducationType }) => {
  return (
    <div>
      <div></div>
    </div>
  );
};
