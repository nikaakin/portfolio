import { ExperienceType, ThemePropType } from "@/types";

export const ExperienceCard = ({
  experience,
  theme,
}: ThemePropType & {
  experience: ExperienceType;
}) => {
  return (
    <div>
      <div></div>
    </div>
  );
};
